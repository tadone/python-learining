#! python3
# Create function

listOfLists = [['apples', 'oranges', 'cherries', 'banana'],
             ['Alice', 'Bob', 'Carol', 'David'],
             ['dogs', 'cats', 'moose', 'goose']]

def printTable(list, _width):
    colWidths = [0] * len(listOfLists) # Number of lists inside
    for i in range(len(listOfLists)):
        print(stlistOfLists[i]).rjust(_width))
        print('New List')

printTable(listOfLists, 10)
